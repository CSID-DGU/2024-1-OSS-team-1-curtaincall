import { createTheme } from '@mui/material/styles';

export const DBTheme = createTheme({
    palette: {
        primary: {
            main: '#ff4d4d',
        },
        secondary: {
            main: '#ffa793',
        },
    },
});